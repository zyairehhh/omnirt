# ADR 0001：v0.1 范围

## 状态

已接受

## 决策

`omnirt` v0.1 聚焦于最小可用的图像 / 视频运行时抽象，并优先支持 CUDA 与 Ascend 后端。服务能力、adapter 热切换和图级优化被有意延后。

## 后果

- 代码库会优先优化清晰的组件边界和后端边界
- 较重的模型接线工作可以逐步落地，而不需要频繁重做公开 request/result 契约
- 测试体系从第一天开始就划分为 unit、integration、parity 三层
