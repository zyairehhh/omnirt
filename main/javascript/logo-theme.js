// Intentionally minimal: CSS handles logo light/dark switching via
// [data-md-color-scheme] selectors on .logo-light / .logo-dark images.
// This file exists so mkdocs.yml can reference `extra_javascript` without
// breaking; add richer behavior here later if needed.
